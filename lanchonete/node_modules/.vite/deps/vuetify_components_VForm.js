import { Kn as ref, W as createBaseVNode, ar as normalizeClass, sr as normalizeStyle } from "./vue.runtime.esm-bundler-CJPQuahF.js";
import { l as propsFactory, n as genericComponent } from "./defineComponent-B0sBHKl2.js";
import { n as makeComponentProps, t as useRender } from "./useRender-D80NMc4X.js";
import { t as forwardRefs } from "./forwardRefs-DR1BPrru.js";
import { n as makeFormProps, t as createForm } from "./form-pWtlNCmR.js";
//#region node_modules/vuetify/lib/components/VForm/VForm.js
var makeVFormProps = propsFactory({
	...makeComponentProps(),
	...makeFormProps()
}, "VForm");
var VForm = genericComponent()({
	name: "VForm",
	props: makeVFormProps(),
	emits: {
		"update:modelValue": (val) => true,
		submit: (e) => true
	},
	setup(props, { slots, emit }) {
		const form = createForm(props);
		const formRef = ref();
		function onReset(e) {
			e.preventDefault();
			form.reset();
		}
		function onSubmit(_e) {
			const e = _e;
			const ready = form.validate();
			e.then = ready.then.bind(ready);
			e.catch = ready.catch.bind(ready);
			e.finally = ready.finally.bind(ready);
			emit("submit", e);
			if (!e.defaultPrevented) ready.then(({ valid }) => {
				if (valid) formRef.value?.submit();
			});
			e.preventDefault();
		}
		useRender(() => createBaseVNode("form", {
			"ref": formRef,
			"class": normalizeClass(["v-form", props.class]),
			"style": normalizeStyle(props.style),
			"novalidate": true,
			"onReset": onReset,
			"onSubmit": onSubmit
		}, [slots.default?.({
			get errors() {
				return form.errors.value;
			},
			get isDisabled() {
				return form.isDisabled.value;
			},
			get isReadonly() {
				return form.isReadonly.value;
			},
			get isValidating() {
				return form.isValidating.value;
			},
			get isValid() {
				return form.isValid.value;
			},
			get items() {
				return form.items.value;
			},
			validate: form.validate,
			reset: form.reset,
			resetValidation: form.resetValidation
		})]));
		return forwardRefs(form, formRef);
	}
});
//#endregion
export { VForm };

//# sourceMappingURL=vuetify_components_VForm.js.map